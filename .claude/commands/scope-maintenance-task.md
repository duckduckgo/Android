> **How to use:** Type `/scope-maintenance-task` in Claude Code from the Android repo root.
> No arguments required — the agent will ask you questions interactively.

Launch the `android-maintenance-scope` agent to help scope a maintenance idea into a
properly formatted task for the Android Agentic Maintenance Backlog.
