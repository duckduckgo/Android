appId: com.duckduckgo.mobile.android.debug
name: "UnifiedInput: ask duck.ai suggestion opens chat × omnibar positions"
tags:
  - unifiedInputTest
---
# 1/3 — omnibar top
- retry:
    maxRetries: 3
    commands:
      - launchApp:
          clearState: true
          stopApp: true
          arguments:
            isMaestro: "true"
            nativeInputToggle: "true"
            inputWithAiToggle: "true"
            omnibarPosition: "top"
      - runFlow: ../shared/skip_all_onboarding.yaml
      - runFlow: shared/flow_ask_duckai_suggestion.yaml

# 2/3 — omnibar bottom
- retry:
    maxRetries: 3
    commands:
      - launchApp:
          clearState: true
          stopApp: true
          arguments:
            isMaestro: "true"
            nativeInputToggle: "true"
            inputWithAiToggle: "true"
            omnibarPosition: "bottom"
      - runFlow: ../shared/skip_all_onboarding.yaml
      - runFlow: shared/flow_ask_duckai_suggestion.yaml

# 3/3 — omnibar split
- retry:
    maxRetries: 3
    commands:
      - launchApp:
          clearState: true
          stopApp: true
          arguments:
            isMaestro: "true"
            nativeInputToggle: "true"
            inputWithAiToggle: "true"
            omnibarPosition: "split"
      - runFlow: ../shared/skip_all_onboarding.yaml
      - runFlow: shared/flow_ask_duckai_suggestion.yaml
