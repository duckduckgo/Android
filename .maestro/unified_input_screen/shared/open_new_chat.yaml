appId: com.duckduckgo.mobile.android
---
# move to Duck chat mode \u2014 target the tab by id to avoid Maestro picking up
# any other "Duck.ai" text on screen
- tapOn:
    id: "tabDuckAi"
- assertVisible: "Ask anything privately\u2026"

# verify that Duck.ai web view is opened when submitting a query in chat mode
- tapOn:
    id: "inputField"
- inputText: "what is a car?"
- tapOn:
    id: "actionSend"

# accept T&C if visible
- assertVisible:
    optional: true
    text: 'Agree and Continue'
- tapOn:
    optional: true
    text: "Agree and Continue"

# duck.ai's web view sometimes fails to load — tap "Reload Duck.ai" if shown
- runFlow: reload_duckai_if_needed.yaml

# move to search mode \u2014 target the tab by id; `tapOn: "Search"` was matching
# the keyboard's magnifying-glass action key instead of the Search tab
- tapOn: "Ask anything privately\u2026"
- tapOn:
    id: "tabSearch"
- inputText: "back to search"
- tapOn: "back to search"
- pressKey: enter

# wait for the SERP to load \u2014 daxIcon and the browser container appear together
# once rendering finishes; CI is slower than a local device so give it room
- extendedWaitUntil:
    visible:
      id: "browserLayout"
    timeout: 20000
- extendedWaitUntil:
    visible:
      id: "daxIcon"
    timeout: 10000
- assertVisible:
    id: "daxIcon"
- assertVisible:
    id: "browserLayout"