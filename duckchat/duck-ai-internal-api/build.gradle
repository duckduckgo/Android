plugins {
    id 'com.android.library'
    id 'kotlin-android'
}

apply from: "$rootProject.projectDir/gradle/android-library.gradle"

android {
    namespace 'com.duckduckgo.duckchat.internalapi'
}

dependencies {
    // intentionally empty — interface module only
}
