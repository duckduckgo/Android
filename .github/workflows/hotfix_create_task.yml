name: Hotfix - Create Hotfix Task in Asana

on:
  workflow_call:
    inputs:
      app-version:
        description: 'Hotfix version to release (e.g. 5.284.1)'
        required: true
        type: string
      commit-hashes:
        description: 'Commit SHA(s) cherry-picked into the hotfix (space- or comma-separated)'
        required: true
        type: string
  workflow_dispatch:
    inputs:
      app-version:
        description: 'Hotfix version to release (e.g. 5.284.1)'
        required: true
        type: string
      commit-hashes:
        description: 'Commit SHA(s) cherry-picked into the hotfix (space- or comma-separated)'
        required: true
        type: string

env:
  ASANA_PAT: ${{ secrets.ASANA_ACCESS_TOKEN }}
  emoji_info: ":information_source:" # ℹ️

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  create_hotfix_task:
    name: Create Android App Hotfix Task in Asana
    runs-on: macos-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install dependencies for create-asana-hotfix-release task
        run: |
          python -m pip install --upgrade pip
          pip install -r scripts/release/requirements.txt

      - name: Create Asana hotfix task
        env:
          ASANA_ACCESS_TOKEN: ${{ secrets.ASANA_ACCESS_TOKEN }}
        shell: bash
        run: |
          set -eo pipefail
          RELEASE_TASK_URL=$(python ./scripts/release/create-asana-hotfix-release.py \
          --android-repo-path . \
          --tag "${{ inputs.app-version }}" \
          --commit-hashes "${{ inputs.commit-hashes }}" \
          --trigger-phrase 'Task/Issue URL:' \
          --asana-project-id "${{ vars.GH_ANDROID_RELEASE_BOARD_PROJECT_ID }}" \
          --asana-section-id "${{ vars.GH_ANDROID_RELEASE_BOARD_QA_SECTION_ID }}" \
          --asana-workspace-id "${{ secrets.GH_ASANA_WORKSPACE_ID }}" \
          --asana-api-key-env-var ASANA_ACCESS_TOKEN \
          --template-task-id "${{ vars.GH_ANDROID_RELEASE_BOARD_TEMPLATE_TASK_ID }}")
          echo "RELEASE_TASK_URL=$RELEASE_TASK_URL" >> "$GITHUB_ENV"

      - name: Assign task to Github Actor
        id: assign-hotfix-task
        uses: ./.github/actions/assign-release-task
        with:
          task_name: 'Android Release ${{ inputs.app-version }}'
          asana_token: ${{ secrets.ASANA_ACCESS_TOKEN }}
          project_gid: ${{ vars.GH_ANDROID_RELEASE_BOARD_PROJECT_ID }}
          github_token: ${{ secrets.DAXMOBILE_ANDROID_AUTOMATION }}

      - name: Get Asana Task Permalink
        id: get-task-permalink
        uses: duckduckgo/native-github-asana-sync@v2.6.0
        with:
          asana-pat: ${{ secrets.ASANA_ACCESS_TOKEN }}
          asana-task-id: ${{ steps.assign-hotfix-task.outputs.task_id }}
          action: 'get-asana-task-permalink'

      - name: Notify Mattermost of Task created
        id: send-mm-task-created
        uses: duckduckgo/native-github-asana-sync@v2.6.0
        with:
          mattermost-token: ${{ secrets.MM_AUTH_TOKEN }}
          mattermost-team-id: ${{ secrets.MM_TEAM_ID }}
          mattermost-channel-name: ${{ vars.MM_RELEASE_NOTIFY_CHANNEL }}
          mattermost-message: ${{env.emoji_info}} Hotfix task created successfully ${{ steps.get-task-permalink.outputs.asanaTaskPermalink }}
          action: 'send-mattermost-message'

      - name: Create Asana task when workflow failed
        if: ${{ failure() }}
        uses: duckduckgo/native-github-asana-sync@v2.6.0
        with:
          asana-pat: ${{ secrets.ASANA_ACCESS_TOKEN }}
          asana-project: ${{ vars.GH_ANDROID_APP_RELEASES_PROJECT_ID }}
          asana-section: ${{ vars.GH_ANDROID_APP_RELEASES_PROJECT_BLOCKER_SECTION_ID }}
          asana-task-name: GH Workflow Failure - Create Android App Hotfix Task
          asana-task-description: The Create Android App Hotfix Task workflow has failed. See https://github.com/duckduckgo/Android/actions/runs/${{ github.run_id }}
          action: 'create-asana-task'

      - name: Notify Mattermost when workflow failed
        if: ${{ failure() }}
        id: send-mm-message-error
        uses: duckduckgo/native-github-asana-sync@v2.6.0
        with:
          mattermost-token: ${{ secrets.MM_AUTH_TOKEN }}
          mattermost-team-id: ${{ secrets.MM_TEAM_ID }}
          mattermost-channel-name: ${{ vars.MM_RELEASE_NOTIFY_CHANNEL }}
          asana-task-description: The Create Android App Hotfix Task workflow has failed. See https://github.com/duckduckgo/Android/actions/runs/${{ github.run_id }}
          action: 'send-mattermost-message'
