name: Add a comment

on:
  pull_request:
    types: [opened, edited, labeled, unlabeled]

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: aitorvs/github-asana-action@v0.1.1
        with:
          asana-pat: ${{ secrets.GH_ASANA_SECRET }}
          task-comment: 'View Pull Request Here: '
          trigger-phrase: "Task/Issue URL:"