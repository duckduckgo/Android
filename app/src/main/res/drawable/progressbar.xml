<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Define the background properties like color etc -->
    <item android:id="@android:id/background">
        <shape>
            <gradient
                android:startColor="@color/actionbar_progressbar_background"
                android:centerColor="@color/actionbar_progressbar_background"
                android:centerY="1.0"
                android:endColor="@color/actionbar_progressbar_background"
                android:angle="270"
                />
        </shape>
    </item>

    <!-- Define the progress properties like start color, end color etc -->
    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <gradient
                    android:startColor="@color/actionbar_progressbar_color"
                    android:centerColor="@color/actionbar_progressbar_color"
                    android:centerY="1.0"
                    android:endColor="@color/actionbar_progressbar_color"
                    android:angle="270"
                    />
            </shape>
        </clip>
    </item>

</layer-list>